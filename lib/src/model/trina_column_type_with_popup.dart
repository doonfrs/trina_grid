/// An interface that describes a column that use a popup to select a value.
abstract class TrinaColumnTypeWithPopup {
  /// Whether the popup should value is selected.
  bool get selectWithSingleTap;
}
